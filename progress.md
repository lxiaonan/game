Original prompt: 帮我制作一个以第一人称是熊二为人物的角色进行森林探索,可以自己移动的第一人称游戏,需要背景和人物真实,并且里面还有很多邻居,你开启制作吧,可以使用模型生图

# 制作进度

## 已完成的里程碑

1. **打通生图接口** — `https://image.mlgb7.com/v1/images/generations`,模型 `gpt-image-2.5-sunburst`。
   注意:必须带浏览器 User-Agent,否则 Cloudflare 返回 `403 error code: 1010`;5 并发可用,单张约 45~60 秒。
2. **生成 19 张素材** — 天空全景、草地/泥土(可平铺)、3 种树、灌木、倒木、巨石、小木屋、松果、6 个角色、熊二头像、第一人称熊掌。
3. **贴图后处理** (`tools/postprocess.py`) — 白底抠图(scipy 连通域 + 边缘泛洪 + 去白边 + 羽化)、纹理无缝化(offset+crossfade)、等比缩放。
4. **游戏本体** — three.js r160 第一人称探索:天空穹顶、雾、草地与小径、约 520 棵树 + 300 灌木(全部面向相机的公告板精灵,随机镜像)、木屋、营火、巨石、12 位会自己溜达的邻居、7 颗会自动拾取的金松果、任务与结局、小地图、程序化音效、手机触屏操作。
5. **自动化验证** — 用 `develop-web-game` 的 Playwright 客户端逐项验证并通过。

## 修过的关键 Bug

- **相机朝向与移动方向相反**:three.js 相机看向 -Z,而移动向量用的是 `(sin yaw, cos yaw)`,导致整片森林一直在玩家背后。改为 `camera.rotateY(yaw + Math.PI)`。
- **整片森林悬空**:精灵锚点设为底部对齐(`center.y = 0`),但 Y 坐标又加了半个树高。改为 Y = 0。
- **营地道具过大**:倒木/木屋按图片高度缩放,而原图是横向长图,导致 20 米长的木头糊满屏幕。按正确尺寸重新标定。
- **花粉粒子糊镜头**:粒子飘到镜头前变成大灰斑。改为跟着玩家循环包裹的粒子盒,并排斥 2.4 米内的粒子。
- **任务计数不刷新**:捡到松果后没有调用 `ui.setQuest`。
- **名字牌太大**:缩小到 1.55×0.39,并做成 4.8~20 米的淡入淡出。
- **指针锁定报错**:`requestPointerLock()` 的 Promise 被拒绝时未捕获。
- **小径整条消失**:路面条带几何体的三角面绕序反了,法线朝下,从上方看全被背面剔除。修正绕序并加了一层更宽的半透明路面让边缘柔和。
- **隔着身后也能触发对话**:交互提示改为只对朝向前方 80° 内的邻居出现。

## 后续可做的方向(未做)

- 更多任务与收集品(蜂蜜罐、蘑菇),或把金松果换成熊二的蜂蜜罐主题。
- 昼夜循环与夜晚萤火虫(天空贴图已经有可用的素材,只差光照与雾的时间插值)。
- 邻居各自的小剧情、送礼与好感度。
- 用 InstancedMesh 或合并几何体进一步降低绘制调用(目前可见绘制调用约 246,已经不高)。

## 测试记录(全部通过)

| 场景 | 结果 |
| --- | --- |
| 开始界面 → 点击开始 | 进入游戏,mode=playing |
| 走路 / 转身 / 平移 | 位置与朝向按预期变化 |
| 跳跃 | 离地后正常落回 y=0 |
| 撞木屋 | 被推到墙面外(z 停在 11.02,不再前进) |
| 走到熊大身边 | 出现「按 E 和 熊大 说话」提示 |
| 对话 | 名字、打字机文本、逐行推进、二次对话走 after 台词正常 |
| 捡松果 | 计数 0→1,HUD 进度条与提示同步 |
| 集齐 7 颗交给熊大 | 触发结局界面,fps/绘制调用正常 |
| Esc 暂停 / E 恢复 | mode 在 paused / playing 之间正确切换 |
| 站在小径上 | 泥土小径正确渲染,边缘与草地自然过渡 |
| 控制台 | 无 JS 报错(仅 headless 软件渲染的指针锁定与音频自动播放警告) |

测试用的浏览器无头软件渲染只有约 21fps,真实显卡下不受影响(可见绘制调用 246、三角面 2.5 万)。

## 内存与体积(2026-09-22)

玩家问"这么简单的游戏为什么占这么大内存"。实测(`node tools/mem.mjs`),1600×900 / MSAA 4x:

| 项目 | 优化前 | 优化后 |
| --- | --- | --- |
| 纹理显存(含 mipmap) | 84.4 MB | **59.9 MB** |
| 绘制缓冲(含 MSAA) | 27.5 MB | 27.5 MB |
| JS 堆 | 6~9 MB | **5~12 MB** |
| DOM 节点 / 监听 | 412 / 107 | 411 / 103 |
| 游戏自身合计 | ≈ 112 MB | **≈ 87 MB** |

结论:**代码侧几乎不占内存(JS 堆不到 12MB),大头是美术贴图**。优化过程没有增加内存 —— 反而少了 870 个对象、500 次 draw call。任务管理器里看到的大数字主要是 Chrome 每个标签页自身的基础开销,跟页面内容无关。

找到并处理的问题:

1. **五张 1024² 平铺贴图严重过采样**。草地一张平铺对应 3.2 米世界、1024 像素横跨,即 3mm/texel —— 比屏幕能分辨的细一个数量级。在加载时把 grass/dirt/water/bark/cliff 缩到 512²、天空缩到 1024(原图文件不动,只改上传的尺寸),纹理显存 84.4 → 59.9 MB。截图对比:亮度相关 0.982~0.998、绿色像素比 0.985~1.001,观感无变化。
2. **我自己的测试残留占了 522 MB 磁盘**(Chrome 的 `.perf-profile-*` 配置目录 + 93 张调试截图),已全部删除。项目从 694MB 降到 172MB。
3. `assets/raw` 53MB 是 AI 生图原稿,**运行时不加载**,只占磁盘;`node_modules` 17.7MB 只是自动化测试用的 Playwright,玩游戏不需要(只要 Python);`%SystemDrive%` 10.8MB 是别的工具把 `%SystemDrive%` 变量展开失败后复制进来的系统缓存垃圾,与项目无关。

一个**需要更正的判断**:我原以为 `texture.clone()` 会重复占用显存(旧代码给同一张图不同 `repeat` 时克隆了 8 次),据此把平铺倍数烘进几何体 UV。后来查 `vendor/three.module.js` 的 `WebGLTextures` 发现它按 `texture.source` 缓存上传,而 `clone()` 共享同一个 source —— **克隆本来就不会重复上传显存**,那步改动实际省下 0MB。UV 烘焙本身是等价的(`uv * repeat + offset` 两种写法一致),代码也更干净,就保留了;但"省 43MB"是我一开始算错的,以实测为准。同时也修了这个诊断指标(改为按 source 去重计数)。

还想更省的话:`?aa=0` 关掉 MSAA 能再省约 20MB 绘制缓冲(1600×900 下 27.5MB,1920×1080 下约 40MB);6 张角色贴图(545~749×1024,约 20MB)也可以减半,但近距离对话时会看出来,没动。

## 部署可行性(2026-09-22)

问"能不能部署到 2 核 2G 的服务器"。答案:**能,而且 2核2G 是严重过剩**。

这是纯静态项目,服务端不做任何计算(没有后端/数据库/构建步骤)。实测 Python 自带 `http.server` 托管整个游戏:
**工作集 10.1MB、1 个线程、全程累计 5.69 秒 CPU**。就算换成"同时几个玩家在线",每个连接也只是读文件发出去,nginx 用 `sendfile` 是零拷贝,
28MB 的资源在 2G 内存的 page cache 里随便放。1 核 256M 都够。

真正会卡人的是客户端和带宽,不是服务器:

| 约束 | 实测 |
| --- | --- |
| 首屏传输 | 37 个请求 / 28.48MB(其中 PNG 27.14MB)。100Mbps 约 2.5 秒,5Mbps 约 45 秒 |
| 客户端 | 需要 WebGL2。负载全在客户端显卡上,核显可以加 `?aa=0` |

准备的东西:

- `tools/pack.mjs` → `dist/`:只有 `index.html`/`src`/`vendor`/`assets/tex`,**38 个文件 28.1MB**。
  排除掉 `assets/raw` 53MB(AI 原稿)、`tools` 65.8MB、`node_modules` 17.7MB —— 浏览器一个都不会请求。
- `deploy/nginx.conf`:文本 gzip、贴图与 js 分级长缓存、`sendfile` + `open_file_cache`。
- `start_game.py` 现在支持 `python start_game.py 0.0.0.0 8000`:可以监听公网网卡,且监听非回环地址时不再尝试打开浏览器(原来硬编码 127.0.0.1,外部根本访问不到)。

**还能再降一个数量级**:实测把最大的 6 张贴图转 WebP q85,12.2MB → 2.0MB(降到 16%)。全部贴图照此推算约 26.7MB → 4~5MB,
首屏从 28MB 降到约 6MB。但这会**有损重编码美术文件**,没有擅自做 —— 想要就说一声。

## GitHub Pages 部署(2026-09-22)

问"能不能部署到 GitHub Pages"。答案:**能,不用改任何代码**,已按 Pages 项目站点的形式实测通过。

关键是 Pages 的两个坑,都查过了:

1. **项目站点在子路径 `/‹仓库名›/` 下,不是域名根目录** —— 任何以 `/` 开头的绝对路径都会 404。
   `tools/pathcheck.py` 检查了 36 个引用:**全部相对路径**。模块之间用 `../vendor/three.module.js`、
   贴图用 `assets/tex/…`(相对文档),所以放在任意子路径下都能解析。
2. **Pages 跑在 Linux 上,文件名区分大小写**,而 Windows 不区分 —— 本地跑得好好的,推上去可能 404。
   同样这 36 个引用逐个做了大小写精确比对,全部一致。

另外 `.js` 的 MIME 也验了(Pages 返回正确的 JS 类型,否则 ES module 会被拒绝执行)。

准备的东西:

- `.github/workflows/pages.yml` —— 只把 `tools/pack.mjs` 挑出的 28MB 传上去,仓库里可以保留生图原稿和测试工具。
  也可以不用 Action,直接把仓库根目录当站点发布(`Settings → Pages → Deploy from a branch → main / (root)`)。
- `tools/pack.mjs` 现在会写入 `.nojekyll`(Pages 默认跑 Jekyll,会跳过下划线开头的文件/目录)。
- `.gitignore` —— 排除 `node_modules/`、`dist/`、测试截图等。
- `tools/deploycheck.mjs` —— **纯 HTTP 的部署自检**,不开浏览器:
  逐个请求页面上所有引用,检查 404、大小写不符、跳出子路径的绝对路径、`.js` 的 MIME 类型。
  实测 `http://127.0.0.1:8910/my-repo/index.html`(模拟子路径站点):36 个引用 / 28.1MB 全部 200 通过。

体积上限也够宽:单文件最大 2.42MB(Pages 上限 100MB/文件),站点 28MB(上限 1GB)。
Pages 自带 CDN + HTTPS,所以 2核2G 那台服务器可以先不买。

## 推送到 GitHub(2026-09-22)

目标仓库 `https://github.com/lxiaonan/game.git`(当时是空仓库)。

本地已经全部准备好,只差一次认证:

- `git init -b main`,单个提交 `38b8cb9`,**79 个文件 / 27.8 MB**,工作区干净。
- `.gitignore` 排除了 `node_modules/`、`dist/`、`tools/shots/`(58MB 调试截图)、
  `assets/raw/`(53MB AI 生图原稿)、各种浏览器测试 profile、`%SystemDrive%/` 垃圾目录。
- 提交内容是**仓库根目录即站点**的布局(`index.html`、`src/`、`vendor/`、`assets/tex/` 相对路径都对),
  所以 Pages 直接选 `main / (root)` 即可,不需要 Action、不需要构建。
- 远端已设为 `origin`。

**我推不了**——这台机器上没有任何可用的 GitHub 凭据:
`git credential fill` 查了三种 URL 形式都取不到密码(凭据管理器里只有个 `git:https://github.com` 空壳条目),
`~/.ssh` 里只有 `known_hosts` 没有密钥,也没装 `gh`。SSH 试连 github 是 `Permission denied (publickey)`。
认证必须由账号本人做一次。

所以给了个双击就能跑的脚本:

- `推送到GitHub.bat` —— **纯 ASCII**,只负责用 PowerShell 5.1 启动下面的脚本。
  踩过一个坑:第一版把中文直接写在 `.bat` 里,cmd 会把 UTF-8 汉字的字节当成元字符,
  整个脚本被解析得支离破碎(`'Personal' is not recognized`、`'t-scm.com' is not recognized`)。
  现在中文全部移到 ps1,并且**必须存成 UTF-8 带 BOM**,否则 Windows PowerShell 5.1 读出来是乱码。
- `tools/push_to_github.ps1` —— 提交 + 推送 + 失败时给出排查提示(窗口被关 / 账号不对 / 该用 token)。

已用 PowerShell 5.1 实测:中文正常、无凭据时 2.8 秒内清晰报错、不卡住不弹窗。

## 新手任务(已接上)

光头强在河对岸砍树。熊大把任务拆成:找熊大,走过独木桥,按 F 把光头强赶走,回去报喜,再找 7 颗金松果。

独木桥原先多转了 90 度,横在河道中间挡住视线。现在圆木沿东西向架在两岸之间,桥面可走,两侧掉下去会被冲回岸上。崖壁在桥位留了缺口,桥柱从河床撑到圆木。

## 性能优化(2026-09-22)

玩家反馈"进游戏很卡"。实测环境:Intel Arc 140T 核显(共享显存、吃带宽),1920×1080。

定位到的真实原因(按影响排序):

1. **约 870 个独立 `THREE.Sprite`** — 每个精灵一次 draw call,而且每帧都在 CPU 上重写 scale 来做摇摆。实测主线程占用 98%(不锁帧时),完全没有余量,任何一次 GC 或系统调度都会掉帧。帧内可见 draw call 高达 517。
2. **全屏 CSS 合成开销** — `#loading`/`#start`/`#pause`/`#win` 四个全屏元素带 `backdrop-filter: blur(7px)`,隐藏时只设了 `opacity: 0`,模糊表面依然存在;6 个 `.panel` 也有 `blur(9px)`;`.vignette` 用 `mix-blend-mode: multiply` 强制合成器每帧回读并重新混合整屏;再叠一层全屏 `.grain`。
3. **每帧 JS 浪费** — 830 个对象的摇摆循环、420 个花粉粒子的逐粒子 BufferAttribute 读写 + 每帧 5KB 上传、每帧重画整个小地图(渐变 + 数百个路径点 + 每帧 `ctx.font` 解析与文字布局)、每帧写 DOM 文本(触发重排)。
4. **全屏地面用 `MeshStandardMaterial`**(PBR BRDF + IBL,而这里既没有环境贴图也不需要金属度/粗糙度),所有贴图统一 `anisotropy = 8` —— 而精灵始终正对相机、UV 导数各向同性,各向异性过滤对它们几乎零收益。
5. **MSAA**:原先开着。优化后帧预算充足,所以**保留**,画面锯齿观感不变。

主要修改:

- `src/billboards.js`(新) — 森林改成按贴图分组的 `InstancedMesh`,公告板与摇摆全部在顶点着色器里做(在视图空间偏移,和 `THREE.Sprite` 内部做法一致),雾距之外的实例收缩成退化顶点直接不参与光栅化。
- `src/pollen.js`(新) — 花粉漂移/绕玩家循环/避让镜头全部 GPU 化,每帧零 JS、零上传。
- `src/world.js` — 树/灌木/岩石/倒木改为批量实例化;地面/水面/河床/崖壁/圆木改用 `MeshPhongMaterial`(保留逐像素光照,营火光晕照旧);`swayers` 只剩 2 个单独精灵。
- `src/ui.js` — 小地图静态层(路径/河流/网格/地标文字)预渲染到离屏 canvas,动态点按 ~10fps 重绘;指南针/追踪器/提示只有内容变化时才写 DOM。
- `index.html` — 移除 4 个隐藏全屏 `backdrop-filter`(`visibility: hidden` 收尾)、面板模糊、`mix-blend-mode`;两层全屏叠加合成一层。乘黑色等于用该 alpha 画黑色,所以暗角观感保持一致。
- `src/main.js` — 像素比上限 2 → 1.5;新增自适应分辨率(按实测刷新率判断,避免被 vsync 误判为"没有余量");`?aa=0`、`?scale=` 调试开关。
- `start_game.py` — 单线程 → 线程化 `ThreadingTCPServer`。单线程服务器会被浏览器空闲的预连接 socket 阻塞,导致页面加载卡住(实测复现过 90 秒不加载)。

**一个顺带发现的坑**:`placeSprite` 里 `mirror` 传的是负的 `scale.x`,但 three.js 精灵着色器用 `length(modelMatrix[0].xyz)` 取模长,**负缩放根本不会镜像** —— 也就是说原代码里"随机镜像一半树木"是死代码,整片森林其实都朝向同一侧。新的实例化着色器一开始真的把一半树翻了,画面因此变化;为了严格保持原样,现在默认不镜像(想要这个变化,传 `createBillboardField({ mirror: true })`)。

结果(同机同场景实测,MSAA 保持开启):

| 指标 | 优化前 | 优化后 |
| --- | --- | --- |
| 可见 draw call | 151 ~ 517(均值 331) | 32 ~ 104(均值 67) |
| 每帧最省耗时(含 GPU drain) | — | 0.5 ~ 0.94ms(均值 0.71ms) |
| 主线程 | 不锁帧时 98% 占满 | 每帧 JS 只剩 step + 少量绘制提交 |

0.71ms/帧 对 60Hz 的 16.7ms 预算是 20 倍以上余量,对 120Hz 的 8.3ms 也接近 12 倍。

验证方式(全部在 `tools/`,可重跑):

- `node tools/perf.mjs <标签> [秒]` — 固定机位测帧耗时/draw call/吞吐。窗口被遮挡时 Chrome 会跳过真实光栅化(会测出假的高帧率),所以每个采样点都等 `document.visibilityState === 'visible'`;这台机器同时开着玩家自己的浏览器,所以报告"最省耗时"而非平均值。
- `node tools/smoke.mjs` — 20 项操作流回归测试(开始/暂停/走路/转身/跳/对话/捡松果/打光头强/独木桥),**全部通过且无页面报错**。
- `python tools/imgdiff.py 旧目录 新目录 机位...` — 结构对比。做了 A/A 对照(同一版本抓两次):差异像素 0.12~0.27%,即截图基本确定性。新旧对比:亮度相关 0.96~0.98、树冠轮廓相关 0.91~0.99、绿色像素比 0.975~0.999,残差集中在贴图边缘(各向异性/材质微差),整体观感不变。


